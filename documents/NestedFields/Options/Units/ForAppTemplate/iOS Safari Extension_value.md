# ForAppTemplate - Value

**Template**: iOS Safari Extension.xctemplate
**Field Path**: `Options[].Units.ForAppTemplate`

## Value

```
{
    Targets =     (
                {
            SharedSettings =             {
                "PRODUCT_BUNDLE_IDENTIFIER" = "___VARIABLE_bundleIdentifier:bundleIdentifier___.Extension";
            };
        }
    );
}
```
