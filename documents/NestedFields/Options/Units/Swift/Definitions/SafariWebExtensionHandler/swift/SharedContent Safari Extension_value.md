# swift - Value

**Template**: SharedContent Safari Extension.xctemplate
**Field Path**: `Options[].Units.Swift.Definitions.SafariWebExtensionHandler.swift`

## Value

```
{
    Path = "SafariWebExtensionHandler.swift";
    TargetIdentifiers =     (
        "com.apple.dt.safariExtension.iOS",
        "com.apple.dt.safariExtension.macOS"
    );
}
```
